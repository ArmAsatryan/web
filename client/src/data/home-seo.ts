/**
 * Homepage SEO copy. Keep `client/index.html` head tags and JSON-LD aligned with these strings.
 * Source of truth: `shared/marketing-seo.ts` (also used for crawler-visible OG injection).
 */
export {
  HOME_PAGE_DESCRIPTION,
  HOME_PAGE_KEYWORDS,
  HOME_PAGE_TITLE,
} from "@shared/marketing-seo";
