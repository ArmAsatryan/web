# Copy to `.env` (gitignored) to override the committed `.env.development`
# / `.env.production` defaults on your machine.
#
# Dev default (see .env.development):  http://192.168.1.25:8080
# Prod default (see .env.production):  https://api.ballistiq.xyz
VITE_API_BASE_URL=http://192.168.1.25:8080
